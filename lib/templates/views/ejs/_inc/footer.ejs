{{bowerFoot}}

<% if ( locals.js ) { %>
	<% js.forEach(function(script){ %>
		<script src="<%= site.dir.js %><%= script %>" />
	<% }) %>
<% } %>

<% if ( locals.environment && environment === 'Development' ) { %>
	//Livereload script rendered
	<script src="http://localhost:35729/livereload.js" />
<% } %>

</body>
</html>